apiVersion: v1
kind: Pod
metadata:
  name: webservice01
spec:
  containers:
  - name: webby
    image: reg.alta3.com/5044-gaddam-webby
    ports:
    - name: web
      containerPort: 8888
      protocol: TCP
